#This is Devops Nginx Demo

This Project Helps you install nginx on linux and show a demo Page

#steps

sudo apt-get update 
sudo apt-install inginx

sudo systemctl start nginx
sudo systemctl enable nginx
